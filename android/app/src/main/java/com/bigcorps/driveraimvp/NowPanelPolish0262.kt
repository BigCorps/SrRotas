package com.srrotas.app

import android.app.Application

/** Legado preservado apenas por compatibilidade binária. RC3.7 não instala nem executa este polish. */
@Deprecated("Substituído pelo NowPanel027037 consolidado")
object NowPanelPolish0262 {
    fun install(@Suppress("UNUSED_PARAMETER") application: Application) = Unit
}
