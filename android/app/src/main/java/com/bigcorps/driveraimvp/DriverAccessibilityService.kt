package com.srrotas.app

/**
 * A API AccessibilityService foi removida do produto na fase 0.12.
 *
 * O fluxo oficial do Sr. Rotas é MediaProjection iniciada explicitamente pelo
 * motorista em cada jornada. Este arquivo permanece apenas como marcador para
 * evitar que instalações/branches antigos reintroduzam silenciosamente o
 * serviço por merge.
 */
@Deprecated("AccessibilityService removido na 0.12; use MediaProjectionOcrService")
internal object DriverAccessibilityServiceRemoved
